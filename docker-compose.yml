version: "3"

services:
  eita-backend:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    depends_on:
      - postgres
    environment:
      DB_HOST: postgres
  postgres:
    image: postgres:13-alpine
    ports:
      - "5432:5432"
